import React from 'react';
import styled from 'styled-components';

const NowLoadingDiv = styled.div`
  font-size: 2em;
  margin: 10px;
`;

const NowLoading = () => {
  return <NowLoadingDiv>Now Loading...</NowLoadingDiv>;
};

export default NowLoading;
