import React, { Fragment } from 'react';

import LoginForm from '../templates/LoginForm';

const Login = () => {
  return (
    <Fragment>
      <LoginForm />
    </Fragment>
  );
};

export default Login;
