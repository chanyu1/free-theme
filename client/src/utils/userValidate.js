// const validate = (values) => {
//   const errors = {};
//   _.each(formFieldTexts, ({ name, noValueError }) => {
//     if (!values[name]) {
//       errors[name] = noValueError;
//     }
//   });
//   return errors;
// };
